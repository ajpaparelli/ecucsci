#include <stdlib.h>
#include "stringtable.h"
#include "allocate.h"

struct Node
{
const char* data;
struct Node* next;
}

Node* stringList;

char* intern(const char* s)
{
  Node* currptr;
  if(stringList != NULL {}
  else
  {
   
}
	

}
